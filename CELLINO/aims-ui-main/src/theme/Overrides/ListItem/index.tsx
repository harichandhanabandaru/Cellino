import { COLORS } from "../../Colors";

const ListItem = {
  MuiListItem: {
    root: {
      "&$selected": {
        backgroundColor: COLORS.GAMMA_BACKGROUND_02,
      },
    },
  },
};

export default ListItem;
