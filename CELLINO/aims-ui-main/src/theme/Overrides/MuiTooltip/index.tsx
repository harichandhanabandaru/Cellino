import { COLORS } from "../../Colors";

const MuiTooltip = {
  styleOverrides: {
    arrow: {
      color: COLORS.BETA_TEXT_HIGH_EMPHASIS,
    },
    tooltip: {
      backgroundColor: COLORS.BETA_TEXT_HIGH_EMPHASIS,
      borderRadius: 8,
      color: COLORS.GAMMA_BACKGROUND_04,
    },
  },
};

export default MuiTooltip;
